# Stage1: fakeroot
# Build under chroot

./configure \
    --prefix=/llvmtools \
    --libdir=/llvmtools/lib/libfakeroot \
    --disable-static \
    --with-ipc=sysv

make -j`nproc --all`

make install
